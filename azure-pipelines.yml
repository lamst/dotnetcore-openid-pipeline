resources:
  repositories:
  - repository: DevOps2019Git
    type: git
    name: TodoList/TodoList
    endpoint: DevOps2019
  
trigger:
  batch: true
  branches:
    include:
    - master
  paths:
    include:
    - TodoListWebApp/*
    exclude:
    - README.md
    - HELP.md

jobs:
- template: build-publish-dotnetcore.yml
  parameters:
    name: BuildWebApp
    project: "**/TodoListWebApp/*.csproj"
    buildConfiguration: "Debug"
    artifactName: "TodoList.WebApp"
    vmImage: "windows-2019"